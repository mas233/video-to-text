# 核心 AI 库
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
safetensors>=0.4.0
sentencepiece>=0.1.99  # Qwen tokenizer 依赖

# ModelScope（国内下载更快，可选）
modelscope>=1.9.0  # 优先从 ModelScope 下载模型

# 量化支持（可选，用于降低显存占用）
bitsandbytes>=0.41.0  # 8-bit/4-bit 量化
# optimum>=1.14.0  # 模型优化（可选）

# 音视频处理
ffmpeg-python>=0.2.0
opencv-python>=4.8.0

# LLM 和 Agent
langchain==0.3.0  # 固定版本，0.3.x 支持 AgentExecutor
langchain-core==0.3.0
langchain-community==0.3.0  # HuggingFacePipeline 需要
langchain-text-splitters==0.3.0
pydantic>=2.0.0,<3.0.0

# 注意：已移除 langchain-openai 依赖（改用开源模型）

# MCP (Model Context Protocol)
mcp>=0.9.0

# 可选: 视觉语言模型（需要时安装）
# llava-next>=0.1.0
# qwen-vl>=0.1.0
librosa
